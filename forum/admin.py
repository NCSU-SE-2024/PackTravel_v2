"""
This module configures the admin interface for the Forum app in the Django project.

To register models for the admin interface, use the `admin.site.register()` method
to make them accessible and manageable through the Django admin panel.
"""

from django.contrib import admin

# Register your models here.
